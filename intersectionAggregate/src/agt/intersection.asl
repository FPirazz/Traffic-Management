!initIntersection.

+!initIntersection
    <- println("Setting up Intersection...");
       makeArtifact("intersection", "acme.Intersection", [], Int1);
       focus(Int1);
       println("Intersection Ready!").
