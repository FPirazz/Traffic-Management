apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: vue-app
  name: vue-app
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 80
      nodePort: 30000  # Sostituisci con la porta del nodo desiderata
  selector:
    io.kompose.service: vue-app
  type: NodePort