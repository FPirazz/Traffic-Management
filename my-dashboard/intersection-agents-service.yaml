apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: intersection-agents
  name: intersection-agents
spec:
  clusterIP: 10.99.235.130
  ports:
    - name: "9080"
      port: 9080
      targetPort: 9080
  selector:
    io.kompose.service: intersection-agents
  type: LoadBalancer  # Aggiungi questa riga per specificare il tipo NodePort